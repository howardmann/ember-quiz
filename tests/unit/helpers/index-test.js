
import { index } from 'ember-quiz/helpers/index';
import { module, test } from 'qunit';

module('Unit | Helper | index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = index([42]);
  assert.ok(result);
});

